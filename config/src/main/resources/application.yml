spring:
  application:
    name: config
server:
  port: 8888

spring:
  cloud:
    config:
       server:
         git:
           uri: https://gitee.com/ajax168/springCloudConfig/
           searchPaths: /
           username: ajax168
           password: my19841029b
       label: master

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8080/eureka/