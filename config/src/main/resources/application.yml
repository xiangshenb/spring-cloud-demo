spring:
  application:
    name: config
server:
  port: 8888

spring:
  cloud:
    config:
       server:
         git:
           uri: https://gitee.com/ajax168/springCloudConfig/
           searchPaths: /
           username: 123
           password: 123
       label: master

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8080/eureka/